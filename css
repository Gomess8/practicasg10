body {
  font-family: Arial, sans-serif;
  margin: 2em;
  background-color: #f5f5f5;
  color: #333;
}

input, button {
  padding: 0.5em;
  font-size: 1em;
}

button {
  margin-left: 0.5em;
  cursor: pointer;
}

#resultado {
  margin-top: 1em;
  font-weight: bold;
}
